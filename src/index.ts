// Public API surface of node-unraid.
// Only the client + its public types are exported here; transport, domain and
// generated internals stay private to keep the published surface small and stable.

export { LIBRARY_NAME } from './support/version.js';
